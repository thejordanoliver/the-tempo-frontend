import { ExploreWidgetSize } from "types/widgets";

export const EXPLORE_WIDGET_HEIGHTS: Record<ExploreWidgetSize, number> = {
  small: 220,
  medium: 300,
  large: 420,
};

export const EXPLORE_WIDGET_MIN_HEIGHTS: Record<ExploreWidgetSize, number> = {
  small: 220,
  medium: 300,
  large: 420,
};

export const EXPLORE_WIDGET_MAX_HEIGHTS: Record<ExploreWidgetSize, number> = {
  small: 220,
  medium: 300,
  large: 420,
};
