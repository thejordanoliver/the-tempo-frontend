import { StyleSheet } from "react-native";

export const rosterStyles = StyleSheet.create({
  contentContainer: {
    paddingBottom: 100,
    paddingHorizontal: 12,
    gap: 18,
  },

  playerList: {
    gap: 12,
  },
});
