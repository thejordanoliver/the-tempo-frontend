import { StyleSheet } from "react-native";

export const rosterStyles = StyleSheet.create({
  contentContainer: {
    gap: 18,
    paddingBottom: 100,
    paddingHorizontal: 12,
  },

  playerList: {
    gap: 12,
  },
});
