import { StyleSheet } from "react-native";

export const newsListStyles = (isDark: boolean) =>
  StyleSheet.create({
    container: {
      gap: 12,
      paddingHorizontal: 12,
      paddingBottom: 100,
    },
  });
